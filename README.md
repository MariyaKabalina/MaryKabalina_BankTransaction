Create a folder named Input on your workstation and place a .txt file inside it containing the following information:

Account number from which the transfer is made:
Account number to which the transfer is made:
Transfer amount:
Also, create a folder named Archive.

In the config.properties file, change the paths accordingly
